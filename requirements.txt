# 本项目的外部依赖：
# 1. OpenAI 官方 SDK：既能连 OpenAI，也能连任何 OpenAI 兼容端点（如 DeepSeek、本地 vLLM、Ollama 等）。
# 2. prompt_toolkit：生产级交互输入库（阶段 2 用，阶段 3 已由 Textual 取代，保留供参考）。
# 3. textual：完整 TUI 框架，做漂亮的聊天界面（阶段 3）。
openai>=1.40,<3
prompt_toolkit>=3.0
textual>=0.80
